<%- include('../../shared/header',) %>
<div class="container">
    <!--
    <% if(data.error){ %>
        <div class="alert alert-danger" role="alert">
            <%= datos.error %>
        </div>
    <% } %> 
    -->

    <h1>Create City</h1>

    <form action="/city"
          method="POST"
    >

        <div class="form-group">
            <label form="nameID">Name</label>
            <input
                    id="nameID"
                    type="text"
                    name="name"
                    placeholder="Ej: Quito"
                    title="Enter city name"
                    class="form-control"
            >
            <small id="nombreIDHelp"
                   class="form-text text-muted">
                   the name must be more than 3 characters long
            </small>
        </div>
        <!--
        <div class="form-group">
            <label form="nombreID">Cedula</label>
            <input
                    id="cedula"
                    type="text"
                    name="cedula"
                    placeholder="Ej: 1724175862 "
                    title="Ingresar cedula de usuario"
                    class="form-control"
            >
            <small id="nombreIDHelp"
                   class="form-text text-muted">
                Ingresa el numero de cedula recuerde q esta tiene 10 caracteres
            </small>
        </div>
        -->
        <button type="submit"
                class ="btn btn-success"
        >
            Create City
        </button>
        <!--<button type="reset"
                class="btn btn-secundary">
            Limpiar
        </button>
        <button type="button">
            Buton
        </button>
        -->

    </form>
</div>

<%- include('../../shared/foot-page',) %>

