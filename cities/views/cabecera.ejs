<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EPN U Study</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" href="/public/styles/cabecera.css">
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="/marcas">Motor Store</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="/marcas">Inicio<span class="sr-only">(current)</span></a>
                </li>
                <% if(datos.session.usuario && datos.session.usuario.carrito) {%>
                <li class="nav-item active">
                    <a class="nav-link" href="/carrito">Carrito<span class="sr-only">(current)</span></a>
                </li>
                <% } %>
                <% if(datos.session.usuario && datos.session.usuario.rol=="A") {%>
                <li class="nav-item active">
                    <a class="nav-link" href="/administracion">Aministracion<span class="sr-only">(current)</span></a>
                </li>
                <% } %>
            </ul>
            <% if(!datos.session.usuario){ %>
            <form class="form-inline my-2 my-lg-0">
                <div class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                        data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Invitado
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="/usuario/login">Ingresar</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="/usuario/registry">Registrarse</a>
                    </div>
                </div>
            </form>
            <% } else { %>
            <form class="form-inline my-2 my-lg-0">
                <div class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                        data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <%= datos.session.usuario.nombre %>
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="/api/usuario/logout">Salir</a>
                    </div>
                </div>
            </form>
            <% } %>
        </div>
    </nav>